using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// REGGIE
// Simply moves in a line unless redirected by a safety square in its path
public class Reggie : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
