using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// BASHFUL
// moves in an arbitrary manner on the gameboard, unless approached by a Muncher, in which case he moves away from the Muncher. Became purple in later releases in the series.
public class Bashful : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
